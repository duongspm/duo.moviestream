/**
 * ============================================================
 * COMPONENT: SCROLL TO TOP WITH PROGRESS INDICATOR
 * ============================================================
 */

const ScrollToTop = {
    mount() {
        // Tránh mount trùng lặp nếu hàm được gọi lại
        if (document.getElementById("scroll-to-top")) return;

        // 1. Tạo cấu trúc HTML SVG dạng vòng tròn bao quanh mũi tên lên
        
    }
};